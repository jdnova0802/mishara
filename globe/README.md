# Live Globe (toy)

Not a company. Not Nisaba. Not a SKU.

MapLibre GL JS with `setProjection({ type: 'globe' })` — one renderer from planet to street. OpenFreeMap tiles, no key. Tap country / state / city. Spin stops on drag, wheel, or the button.

```bash
cd globe
npm install
npm run dev
```
