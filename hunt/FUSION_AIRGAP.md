# Claude fusion: clutch × kill-switch air-gap

Same mouth. Sharper *target* if we don’t lie about physics.

**Agree:** Microsoft / ServiceNow-style kill switches are software flags. A compromised agent can talk past a flag. Two independently held halves inside a window, stranger-checkable, is the right *shape* for “software alone cannot authorize go/restart.” Mood board (chain, portcullis, hufu) still fits.

**Do not oversell the bench:** `join/pack.py` today is two strings on **one** computer. That is dual-control, **not** an air-gap. An air-gap means a network attacker never has both halves; join happens at a physical booth / two boxes that don’t share a wire. Until then, Hugging Face agents and this laptop are the same room.

**Don’t aim the clutch at “cut the power / crash the tape.”** That’s a weapon pitch. Capital/circuit-breaker reset is the pipes shop you already turned off. **Do** aim it at: malware cannot **start, reset, or release** a critical go without two physical halves. Same `what` field: boom, pad, hangar, generator *start*, plant *restart* — not a remote kill.

**Occupancy:** safety PLCs, two-key nuclear, trapped-key interlocks, EPO mushroom buttons already exist in steel. Leftover is still: stranger-checkable join + live window + not a skippable software flag. Not a new crypto.

**TikTok:** two keys, then the lights. Less Colossus, more real. Keep the toy boom as the demo. Don’t found on a trillion-dollar sentence.

Separate company. Not Nisaba. No PLC how-to.
