# Site surface — Claude critique → strongest-start rules

**Date:** 2026-08-26  
**Source:** External read of Check / Doors / Trust + Gate buyer surfaces.  
**Action:** Agree where it hurts. Fix surface. Don’t dilute the trust primitive.

---

## Verdict on the critique

| Claim | Ours |
|-------|------|
| BYOK / RFC 3161 / stranger verify + “Velaru cannot forge alone” is the real company sentence | **Agree — keep** |
| Honesty blocks (`their_production: false`, what we don’t claim) are a moat | **Agree — keep** |
| Surface area reads as “nobody home” | **Agree — cut buyer surface to one ladder** |
| Stale drill date on a liveness product is fatal | **Agree — no dead dates on Check/home** |
| Private ontology (weld/fuse/τ/κ…) forces study before buy | **Agree — buyer English first, Gate words second** |
| Eight prices on one primitive = “nothing sold” signal | **Agree — three steps only: drill → Bind Room → weld** |
| $25k+$5k+10bps is procurement, not impulse checkout | **Agree — say that out loud on operator page** |

Private vocabulary + long solo build = same fact twice. That’s the filter tightness showing up as site density. **Strongest start is the cure:** one civilization (PAS/MGA), one paid door ($1,750), daily explain-to-humans.

---

## Buyer ladder (only public commercial story)

1. **Free 60s drill** — soft yes → CHOKE  
2. **Bind Room $1,750** — officer pack (Gate 1 cash)  
3. **Production weld $25k + $5k/mo + 10 bps** — live path block (procurement)

Everything else = lab / `.well-known` / details fold — not nav, not hero.

---

## Copy rule

| First | Then |
|-------|------|
| “We block the bind / payout when clearance isn’t real” | weld, throat, fuse |
| Fail closed · stranger receipt | τ, κ, GAM, POSS-2 |
| Pay Bind Room | Book Bind Room / ontology CTAs |

---

## Ops hygiene

- **No stale “next drill” dates** — use live “last proved” / relative freshness or remove.  
- **Promo Clock (shipped):** `promo_clock.py` · `/.well-known/promo-clock.json` · `/static/promo-clock.js` — past `next_at` fails closed (flip to last proved or hide). Wire Check’s `#drill-bar` with `data-promo-clock` + script (see `PROMO_CLOCK.md`).  
- **Ops Guards (shipped):** `python3 -m gate.ops_guards [--live]` — patent alarm, Stripe Bind Room SKU, buyer lint, Gate 1 counter, live stale-HTML smoke. See `OPS_GUARDS.md`.  
- **One door in outbound** — `gate.velaru.xyz/bind-room` (strongest start).  
- **Don’t lead with $25k** until $1,750 has a path.  
- Check / Doors / Trust (if separate hosts): same ladder, same English, same no-dead-clock rule.

---

## One line

**Keep the honesty primitive. Collapse the museum. Sell the block in their words. Three prices. One vertical until Breath.**

## Commit-auth sentence (front-page candidate)

> Signatures prove a hop occurred. Tickets prove the hop is still allowed to spend, right now, once, for this job and this write.

Not "greater than Ed25519" — Ed25519 is ink; tickets are authorization. See `COMMIT_AUTH.md`.

**Claim grades:** Bind Ticket + epoch lock = stranger-grade today. Exclusion = **map honesty today, not stranger-grade absence** — keep off buyer pages until TSA/root anchored.

**Single-use (verbatim):** Burn on success only, failed redeem retryable, post-burn fail re-issues from fresh LIVE.

**Outbound:** Lead the subject line with the moment — *what can't run once the ticket's gone* — not the crowded "AI governance" category. Costume still AI governance + commit control.
