# Claim grades — what we claim vs refuse

**Publish this.** Diligence asset. Spec: `gate-claim-grades-v1` · `GET /.well-known/claim-grades.json`

## Three grades

| Grade | Meaning | Today |
|-------|---------|--------|
| **stranger** | Shape a stranger can exercise / verify without our narration | Bind Ticket, epoch lock, command radiation, spend protocol, Ed25519 receipts |
| **map honesty** | True inside Gate’s map | Exclusion — *map honesty today, not stranger-grade absence* |
| **refused** | We will not claim | See below |

## `their_production: false` — milestone, not confession

**Unlock (only):** recorded **third-party production weld** with exclusivity attestation (`/production-weld`).

**Does not unlock:** dogfood, Bind Room checkout, drills, demos, first-party skin.

Until then the flag stays false on purpose — pre-third-party-weld, not “nobody wanted this.”

## What we refuse to claim

- `their_production: true` without third-party weld  
- Exclusion as stranger-grade absence before TSA/root  
- New L2 mountain / seeds until paid stranger artifact (Gate 1) — Mouth Ceiling **enforced**  
- Admin CHARGE / soft LIVE resurrect  
- CIC mythology in outbound  

## Burn policy (verbatim)

> Burn on success only, failed redeem retryable, post-burn fail re-issues from fresh LIVE.

## Outbound lead (verbatim)

> What can't run once the ticket's gone — the withdraw that didn't run, and the ticket that's already burned.

## Discovery split

| Surface | URL | For |
|---------|-----|-----|
| Commercial | `/.well-known/gate.json` | Carrier engineer diligence |
| Claim grades | `/.well-known/claim-grades.json` | This document as JSON |
| Lab museum | `/.well-known/lab.json` | Frozen seed/doctrine catalog — not the diligence first stop |
| Commit auth | `/.well-known/commit-auth.json` | Ticket / epoch / exclusion grades |

## Repo note (operational)

GitHub repo is `jdnova0802/mishara`; product is **Gate**. Anyone granted access reads the repo name first — rename / Gate-named entrypoint is a separate ops task, not an invention.
