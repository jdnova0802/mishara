# Visibility — escape Google’s ranking loop

Do **not** try to rank for “AI agents” or “kill switch.” Those terms are owned.
Be found by **agents, AI crawlers, and humans you send a URL to.**

## This week (distribution — Velaru is already public)

1. Show HN — title in `LAUNCH.md` (use velaru.xyz/check until Gate is live)
2. Paste **`/feed`** — agent treasury crossover (spend stream + $handles + clear-before-leave)
3. Paste **`/operator`** to one licensed payout or carrier human (or `/for/operators`)
4. Submit sitemap after Gate is on Render: Google Search Console + Bing
5. Listings: Product Hunt, Indie Hackers, HN, plus live `/.well-known/listings.json` (MCP / x402 / Guidewire / Duck Creek packets — date all, marry one weld)
6. `https://velaru.xyz/llms.txt` — add the same file on Velaru later; Gate has `/llms.txt` now

## Coding that helps (this commit)

- `/feed` — public treasury spend feed (attention × pipe)
- `/t/<handle>` — $handle pages
- `/treasury/claim` + `/v1/treasury/leave` — claim + clear-before-leave
- `/sitemap.xml` — plates + feed
- `/robots.txt`
- `/llms.txt` — AI answer engines (not classic SEO)
- JSON-LD SoftwareApplication on every page
- Already: `/.well-known/gate.json` + `opportunities.json` + `treasury.json`

These only matter **after a public Gate URL**. Until then, distribute **velaru.xyz**.

## What not to code

More museum pages. More stack layers. Competing with Google Search.
