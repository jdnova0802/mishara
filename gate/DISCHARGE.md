# Discharge — standing lapses; the chain does not

A proof rail with **no expiry rail** is a prison. Contributing to a permanent record with no discharge is strictly increasing liability. That is why five AI incident databases stay journalism: nobody self-reports into a subpoena.

Law already has this: statutes of limitation, expungement, bankruptcy, jubilee.  
Digital remaining did not. Jubilee was a name (`P5` / `X6`). This is the receipt.

`/discharge` · `/.well-known/discharge.json` · `POST /discharge/checkout`  
`POST /demo/pas/discharge/schedule` · `/discharge` · `/open` · `/pack`

**$1,500.** We lapse one folio. They open both. They do not implement Gate.

Not a sixth sibling. Not deletion. Not admin CHARGE. Actor cannot self-wipe.

---

## Why this number is not $0

The page number is the SKU. A stranger can pay it this month. That payment **is** Gate 1.

| | |
|--|--|
| **Buyer** | GC / board with a prison folio — no expiry, next contribution blocked |
| **Pain** | A Bind or remaining they cannot contribute to again. Standing never ended. |
| **You sell** | Schedule already lapsed + stranger-openable receipt + folio |
| **Not** | Deletion · immunity · a consortium · Vital night · Space Academy |

One line:

> Your remaining has no expiry. The next contribution is a prison. We lapse the standing and issue the receipt. You open both. $1,500.

Highest *probability* Gate 1 is still Bind **$1,750**. This is the SKU if they already have a stale folio.

---

## Vacancy test (pass)

| Test | Here |
|------|------|
| **Identifier** | `job_id` |
| **Contribution rule** | No schedule → `no_expiry_rail_prison`. The next folio cannot proceed. |
| **Chokepoint** | `may_contribute` is closed until an expiry rail exists. Not a database. |

Standing is **metadata about the record**. The folio stays. A stranger opens both.

---

## What this is not

- Not GDPR theater from a $0 lab  
- Not a memory hole (the journal line still existed)  
- Not issuer-pays assurance (Nisaba issues the lapse; the actor does not forgive themselves)  
- Not Commons-as-founder (Commons still needs a convener)  
- Not immunity. Not a consortium.

---

## Also seated (mouths)

**Near-miss seed** — `POST /demo/pas/commons/near-miss`  
The only layer anyone will contribute: nobody is suing yet. ASRS shape. Not an incident. Not a claim. No checkout.

**Null Remaining** — `/null` **$4,500**  
Failure has a remaining. The killed try is the stock. Checkout is live. See `NULL.md`.

---

## Honest print

Every Bind / Finished / Standing / Night folio **ages into a discharge** or it becomes a liability. The forgetting institution is chronic because new records keep being born.

Do not sell a consortium. Do not fake immunity (ASRS is statutory; we are not Congress).
